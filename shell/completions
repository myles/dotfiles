for file in `ls -l $HOME/.bash_completion.d/*`; do
  source $file
done

for file in `ls -l /etc/bash_completion.d/*`; do
  source $file
done

for file in `ls -l /usr/local/etc/bash_completion.d/*`; do
  source $file
done
