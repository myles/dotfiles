[core]
	excludesfile = /Users/myles/.gitignore
	whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
	attributesfile = /Users/myles/.gitattributes

[user]
	name = Myles Braithwaite
	email = me@mylesbraithwaite.com
	signingkey = 5A2FE7BF

[color]
	ui = auto

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green

[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
	whitespace = red reverse

[color "status"]
	added = yellow
	changed = green
	untracked = cyan

[alias]
	a = commit --ammend
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
	ci = commit
	pick = cherry-pick -s
	sb = status -sb
	log1 = log --pretty=oneline --abbrev-commit
	serve = daemon --reuseaddr --base-path=. --export-all --verbose
	top-commits = shortlog -s -n --no-merges

[github]
	user = myles
	token = be1fbd6617c33c57f904a546ec5df6eefd6cb1cb

[url "git://github.com/"]
	insteadOf = github://

[url "ssh://git@github.com/"]
	pushInsteadOf = github://

[push]
	default = simple
[filter "dropoutput_ipynb"]
	clean = /Users/myles/.bin/ipynb_output_filter.py
	smudge = cat
